---
alwaysApply: true
description: Правила оформления кода, нейминга, обработки ошибок
glob: "src/**/*.ts"
---

# Стандарты кодирования для headset-manager

## Общие
- Язык: TypeScript с включённым `strict: true`.
- Форматирование: использовать Prettier (или следовать стандартному стилю).
- Имена файлов: в формате **camelCase**. Если файл содержит и экспортирует один класс, имя файла должно совпадать с именем класса (PascalCase), например `HeadSetManager.ts`, `WebHidSearcher.ts`. Остальные файлы — camelCase (например, `types.ts`, `codes.ts`).
- Имена папок: в формате **camelCase**. Если основным функционалом папки является экспортируемый класс, папка должна называться так же, как класс (PascalCase), например `HeadSetManager`, `WebHidSearcher`. Остальные папки — camelCase.
- Функции: использовать стрелочные функции (`const fn = () => {}`) вместо объявлений `function`. Методы классов оставлять в виде обычных методов.
- Именование:
  - `camelCase` для переменных, функций, методов.
  - `PascalCase` для классов, интерфейсов, типов, enum.
  - `UPPER_SNAKE_CASE` для констант (коды ошибок, события).
  - Интерфейсы без префикса `I` (например, `HeadSet`, а не `IHeadSet`).
  - Типы с префиксом `T` (например, `TCapabilities`).

## Модули и экспорт
- Использовать ES-модули (`import/export`).
- Публичное API библиотеки экспортировать из `src/index.ts`.
- Внутренние модули не должны экспортироваться наружу.
- Файлы `index.ts` использовать **только** для ре-экспорта: в них не должно быть объявлений типов, логики или реализации — только `export { ... } from '...'` и/или `export type { ... } from '...'`.
- Классы, объекты и функции экспортировать **только** через `export default` в конце файла: не писать `export` в объявлении, в конце файла добавить одну строку `export default ClassName` / `export default config` / `export default fn`. Для классов — объявить класс без `export`, в конце — `export default ClassName`. Для объектов и функций — сохранить в переменную, в конце — `export default variableName`.

## Обработка ошибок
- Определить набор кодов ошибок как enum или константы:
  ```typescript
  export const ErrorCode = {
    DEVICE_NOT_FOUND: 'ID_DEVICE_NOT_FOUND',
    EVENT_NOT_SUPPORTED: 'ID_EVENT_NOT_SUPPORTED',
    // ...
  } as const;
  ```
- Создать классы ошибок, наследующие Error, с полем code.
- Для каждой ошибки создать функцию-предикат в объекте errors:
```typescript
export const errors = {
  hasDeviceNotFoundError: (error: unknown): boolean => {
    return error instanceof DeviceNotFoundError;
  },
  hasEventNotSupportedError: (error: unknown): boolean => {
    return error instanceof EventNotSupportedError;
  },
};
```

В коде выбрасывать только конкретные типы ошибок, не использовать общий Error с текстом.

## Асинхронность
- Все операции, связанные с HID, делать асинхронными (возвращать Promise).
- Использовать async/await, избегать явных .then/.catch, кроме случаев необходимости.

## События
- Использовать пакет **events-constructor**: классы с событиями наследовать от `EventEmitterProxy<TEventMap>`, внутри создавать `new TypedEvents<TEventMap>(EVENT_NAMES)` и передавать в `super()`. Для вызова события использовать `this.events.trigger(eventName, payload)`.
- Типы событий задавать в виде мапы (например, `THeadSetEventMap`): ключ – имя события, значение – тип payload (`void` или `never` для событий без данных).
- Имена событий задавать константой-массивом (например, `HEADSET_EVENT_NAMES`) и использовать для создания `TypedEvents`.

## Тестирование (Jest)
- Файлы тестов рядом с тестируемым модулем: *.test.ts.
- Для моков WebHID использовать встроенные возможности Jest (jest.mock).
- Покрывать основные сценарии: успешный поиск, ошибка если устройство не найдено, подключение, отправка команд, получение событий.
- Тесты должны быть чистыми, без зависимостей от реальных устройств.

## Документация
- Каждый публичный метод/функция должны иметь JSDoc-комментарий с описанием, параметрами, возвращаемым значением и возможными ошибками.
- В корне проекта – README.md с быстрым стартом, ссылкой на демо и схемой mermaid.
- Для демо-страницы – отдельная папка demo с простым HTML и скриптом, подключающим библиотеку.

## Добавление новых правил
- По запросу "Добавь это правило в правила проекта" обновлять данный файл, добавляя соответствующий пункт.
